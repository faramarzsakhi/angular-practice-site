import { NationalCodeDirective } from './national-code.directive';

describe('NationalCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new NationalCodeDirective();
    expect(directive).toBeTruthy();
  });
});
