import { PasswordDifficulityDirective } from './password-difficulity.directive';

describe('PasswordDifficulityDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordDifficulityDirective();
    expect(directive).toBeTruthy();
  });
});
